import { Stack } from "expo-router";
import React from "react";

type Props = {};

const ProcessLayout = (props: Props) => {
  return <Stack screenOptions={{ animation: "fade" }} />;
};

export default ProcessLayout;
